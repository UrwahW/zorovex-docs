# PR-001 — Engineering Review

**Task:** PR-001 (AI Product Completion)  
**Sprint:** Launch Sprint  
**Status:** Pending Architect Approval  
**Depends On:** AI-001 through AI-006, CORE-001, BI-001, UI-001, INTEGRATIONS-001

---

## Summary

Completed the AI Workspace as a production-ready engineering environment. All previously placeholder routes (Knowledge, Behaviour, Goals, Escalations, Settings) are now backed by real APIs and functional UI. The dashboard aggregates runtime and BI metrics. Playground simulations route through the real `AiRuntime` execution pipeline (no `MockRuntime` bypass). Future agent types are hidden (not shown as "Coming Soon").

---

## Backend Services Added

| Service | Purpose |
|---------|---------|
| `AiWorkspace::DashboardService` | AI health, metrics, trends, top goals/knowledge, recent executions |
| `AiWorkspace::KnowledgeService` | Knowledge browser + coverage health |
| `AiWorkspace::SettingsService` | AI defaults, providers, receptionist enablement |
| `AiWorkspace::GoalsService` | Goal registry with usage stats |
| `AiWorkspace::BehaviourService` | Personality, greeting, tone, safety rules |
| `AiWorkspace::EscalationsService` | Pending queue + escalation rules + resolve |
| `AiWorkspace::ObservabilityService` | Runtime trace aggregates + replay |
| `AiWorkspace::EvaluationService` | Prompt testing via runtime + history |

---

## API Endpoints

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/v1/ai/dashboard` | AI workspace dashboard metrics |
| GET/PATCH | `/api/v1/ai/knowledge` | Knowledge browser |
| GET/PATCH | `/api/v1/ai/settings` | AI settings + providers |
| GET/PATCH | `/api/v1/ai/goals` | Goals registry |
| GET/PATCH | `/api/v1/ai/behaviour` | Behaviour configuration |
| GET | `/api/v1/ai/escalations` | Escalation rules + pending queue |
| POST | `/api/v1/ai/escalations/:id/resolve` | Resolve escalation |
| GET | `/api/v1/ai/observability` | Observability snapshot |
| POST | `/api/v1/ai/observability/replay` | Replay execution via runtime |
| GET/POST | `/api/v1/ai/evaluations` | Evaluation history + prompt tests |
| POST | `/api/v1/ai/playground/reset` | Reset playground session |

Existing endpoints retained: agents, receptionist configuration, playground run, runtime execute/logs/providers.

---

## Playground → Runtime

`AiPlayground::Service.run` now calls `AiRuntime.execute` with the receptionist agent. Simulations follow the full pipeline: intent → goals → knowledge → workflow → business actions → response. Results include inspector and workflow metadata.

---

## Frontend

| Route | Status |
|-------|--------|
| `/ai/dashboard` | Operational — metrics, trends, recent executions |
| `/ai/playground` | Operational — real runtime execution |
| `/ai/runtime` | Operational — inspector + logs |
| `/ai/knowledge` | Replaced placeholder |
| `/ai/behaviour` | Replaced placeholder |
| `/ai/goals` | Replaced placeholder |
| `/ai/escalations` | Replaced placeholder |
| `/ai/settings` | Replaced placeholder |
| `/ai/receptionist` | Unchanged — full configuration |

Removed "Coming soon" sidebar section. Voice/Sales agents hidden behind empty feature-flag nav list (not displayed).

---

## Cleanup

- Removed routed placeholder pages from router
- Updated playground response label from "Simulated Runtime" to "AI Runtime"
- Fixed `::Platform::FeatureAccessService` constant resolution in AI controllers

---

## Tests

- `spec/requests/ai_workspace_spec.rb` — dashboard, knowledge, goals, escalations, settings, observability, playground reset
- `spec/requests/ai_playground_spec.rb` — passes with runtime-backed playground
- `spec/requests/ai_runtime_spec.rb` — passes

```bash
DATABASE_URL=postgres://zorovex:zorovex@postgres:5432/zorovex_test \
REDIS_URL=redis://redis:6379/0 \
bundle exec rspec spec/requests/ai_workspace_spec.rb spec/requests/ai_playground_spec.rb spec/requests/ai_runtime_spec.rb
```

---

## Notes / Follow-ups

- Playground simulation library (save/duplicate/export) can extend `SessionStore` persistence in a follow-up
- Evaluation benchmarks and regression datasets are minimal (runtime-backed prompt tests + log history)
- Knowledge upload/reindex UI can deepen integration with business setup flows
- Non-mock LLM providers remain gated until credentials are configured (mock provider is real runtime path)

---

**Stopped for architect approval.**
